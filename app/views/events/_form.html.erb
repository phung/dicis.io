<%= form_for @event, {:class => 'event_submission'} do |f| %>
	<div class="field">
	<%= f.label :name %>:
	<%= f.text_field :name %></br></br>
	</div>
  <div class="actions">
    <%= f.submit 'Start Event'%>
  </div>
<% end %>
